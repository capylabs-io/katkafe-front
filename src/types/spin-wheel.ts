export interface WheelItem {
  label: string;
  image?: string; // Optional image for the item
  imageWidth?: number; // Optional image width
  imageHeight?: number; // Optional image height
  reward?: any;
  color?: string;
}
